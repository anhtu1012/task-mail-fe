/* ================================================================
   Header — Shared Types
   ================================================================ */

export interface BreadcrumbItem {
  label: string;
  href?: string;
  icon?: React.ReactNode;
}
